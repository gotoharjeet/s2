package com.dao;

import com.dto.User;

public class UserDaoImp implements UserDao{

	@Override
	public boolean validateUser(User user) {
		
		if(user.equals(null))	{
			return false;
		}
		else
		{
			return true;
		}
	}

}
